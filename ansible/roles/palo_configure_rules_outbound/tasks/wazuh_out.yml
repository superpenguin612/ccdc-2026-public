---
- name: Allow Outbound Wazuh Communication
  paloaltonetworks.panos.panos_security_rule:
    provider: "{{ panos_provider }}"
    rule_name: "Outbound_WazuhAgent"
    source_zone: ["internal"]
    source_ip: ["Internal_Net"]
    destination_zone: ["external"]
    destination_ip: "{{ groups['svc_wazuh'] | map('extract', hostvars, 'ip_external') | list }}"
    application: "{{ app_type_mappings.fw_wazuh_forwarder.palo_apps }}"
    service: "{{ app_type_mappings.fw_wazuh_forwarder.palo_service }}"
    action: "allow"
    log_setting: "{{ palo_log_setting }}"
  when: # only on the firewall that does NOT protect the wazuh server
    - groups['svc_wazuh'] | default([]) | length > 0
    - groups['svc_wazuh'] | intersect(groups[protected_group] | default([])) | length == 0
