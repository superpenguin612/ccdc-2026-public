---
- name: Allow Outbound HTTP
  paloaltonetworks.panos.panos_security_rule:
    provider: "{{ panos_provider }}"
    rule_name: "Outbound_HTTP"
    source_zone: ["internal"]
    source_ip: ["Internal_Net"]
    destination_zone: ["external"]
    destination_ip: ["any"]
    application: "{{ app_type_mappings.fw_http.palo_apps }}"
    service: "{{ app_type_mappings.fw_http.palo_service }}"
    action: "allow"
