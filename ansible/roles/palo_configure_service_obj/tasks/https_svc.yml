---
- name: Create service-https (TCP 443)
  paloaltonetworks.panos.panos_service_object:
    provider: "{{ panos_provider }}"
    name: "service-https"
    protocol: "tcp"
    destination_port: "443"
